export const metadata = {
    title: 'Notícias de Mundo | Panoramas',
    description: 'Últimas notícias e atualidades sobre Mundo no Panoramas.'
};

export default function mundoLayout({ children }) {
    return children;
}
