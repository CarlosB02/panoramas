export const metadata = {
    title: 'Notícias de Média | Panoramas',
    description: 'Últimas notícias e atualidades sobre Média no Panoramas.'
};

export default function mediaLayout({ children }) {
    return children;
}
