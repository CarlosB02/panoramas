export const metadata = {
    title: 'Notícias de Desporto | Panoramas',
    description: 'Últimas notícias e atualidades sobre Desporto no Panoramas.'
};

export default function desportoLayout({ children }) {
    return children;
}
