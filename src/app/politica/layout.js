export const metadata = {
    title: 'Notícias de Política | Panoramas',
    description: 'Últimas notícias e atualidades sobre Política no Panoramas.'
};

export default function politicaLayout({ children }) {
    return children;
}
